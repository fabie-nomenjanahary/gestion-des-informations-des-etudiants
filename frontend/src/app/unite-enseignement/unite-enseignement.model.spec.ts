import { UniteEnseignement } from './unite-enseignement.model';

describe('UniteEnseignement', () => {
  it('should create an instance', () => {
    expect(new UniteEnseignement()).toBeTruthy();
  });
});
